# Vercel-only requirements — lightweight, no PyTorch/CV
# Total install size: ~80 MB (well under 500 MB limit)

# API & web
fastapi
uvicorn
python-multipart
pydantic
python-dotenv
requests

# AWS DynamoDB
boto3

# Reporting (PDF generation, no CV deps)
reportlab
fpdf2

# Google AI (Gemini — text only, no vision models)
google-genai
